{
  "skill_name": "app-package",
  "skill_path": "app-package",
  "evals": [
    {
      "id": 1,
      "name": "scaffold-hybrid-search-app",
      "prompt": "Scaffold a complete Vespa Cloud application package for hybrid (BM25 + vector) search over 'article' documents. Create all of the following files (write them to their conventional paths within the working directory):\n\n1. services.xml\n   - One container cluster (id='default') exposing the feeding endpoint and the search endpoint.\n   - A built-in hugging-face-embedder component with id 'e5' that loads the e5-small-v2 model from Hugging Face. Use these exact Hugging Face URLs:\n     * transformer-model: https://huggingface.co/intfloat/e5-small-v2/resolve/main/model.onnx\n     * tokenizer-model: https://huggingface.co/intfloat/e5-small-v2/resolve/main/tokenizer.json\n   - One content cluster serving 'article' documents in the standard indexed mode. You choose the cluster id.\n   - Production-grade redundancy.\n   - Vespa Cloud node specifications (use <resources> with vcpu/memory/disk) — node counts of your choice.\n\n2. schemas/article.sd\n   - Fields: title (string, full-text searchable with BM25), body (string, full-text searchable with BM25), embedding (384-dim float tensor) generated by running the body text through the 'e5' embedder defined in services.xml. The embedding field must use HNSW indexing.\n\n3. deployment.xml\n   - A minimal Vespa Cloud deployment.xml deploying to at least one production region.\n\n4. search/query-profiles/types/root.xml\n   - A query profile type that declares a 384-dim float query tensor input named 'q' (so callers can pass a query embedding via ranking.features.query(q)).\n\nMake sure the document type name in services.xml matches the schema file name and the schema/document declarations inside it. Do not create extra unrelated files.",
      "expected_output": "Four files: services.xml (with document-api, search, hugging-face-embedder component referencing e5-small-v2 URLs, content cluster, redundancy declared (either <redundancy> or the preferred <min-redundancy>), document mode='index', Vespa Cloud <resources> specs); schemas/article.sd (with title, body using enable-bm25, and embedding tensor<float>(x[384]) using 'embed e5' in indexing pipeline plus HNSW); deployment.xml (with <prod> and <region>); search/query-profiles/types/root.xml (declaring a 384-dim tensor query input).",
      "files": [],
      "assertions": [
        {"type": "file_exists", "path": "services.xml", "text": "services.xml created"},
        {"type": "file_exists", "path": "*.sd", "text": "Schema file created"},
        {"type": "file_exists", "path": "deployment.xml", "text": "deployment.xml created"},
        {"type": "file_exists", "path": "root.xml", "text": "Query-profile types/root.xml created"},
        {"type": "content_contains", "path": "services.xml", "pattern": "<document-api/>", "text": "<document-api/> declared (required for feeding)"},
        {"type": "content_contains", "path": "services.xml", "pattern": "<search/>", "text": "<search/> endpoint declared"},
        {"type": "content_matches", "path": "services.xml", "pattern": "type=\"hugging-face-embedder\"", "text": "Component declared with type=\"hugging-face-embedder\" (canonical type-shortcut form)"},
        {"type": "content_contains", "path": "services.xml", "pattern": "e5-small-v2", "text": "Embedder uses e5-small-v2 model"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<tokenizer-model\\b[^>]*\\burl=", "text": "<tokenizer-model url=...> element present"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<transformer-model\\b[^>]*\\burl=", "text": "<transformer-model url=...> element present"},
        {"type": "content_matches", "path": "services.xml", "pattern": "mode=\"index\"", "text": "Document mode set explicitly to 'index'"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<(min-)?redundancy>\\s*[1-9]", "text": "redundancy or min-redundancy declared with a positive value"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<resources\\b[^>]*\\bvcpu=", "text": "<resources> with vcpu attribute (Vespa Cloud sizing)"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<document\\b(?=[^>]*\\btype=\"article\")", "text": "Document type 'article' declared (matches schema name)"},
        {"type": "content_contains", "path": "*.sd", "pattern": "schema article", "text": "Schema named 'article'"},
        {"type": "content_contains", "path": "*.sd", "pattern": "document article", "text": "Document declaration named 'article' (matches schema and services.xml)"},
        {"type": "content_contains", "path": "*.sd", "pattern": "embed e5", "text": "Schema indexing pipeline calls 'embed e5'"},
        {"type": "content_contains", "path": "*.sd", "pattern": "x[384]", "text": "Embedding field is 384-dim"},
        {"type": "content_contains", "path": "*.sd", "pattern": "hnsw", "text": "HNSW indexing on embedding"},
        {"type": "content_contains", "path": "*.sd", "pattern": "enable-bm25", "text": "enable-bm25 on text fields used for BM25 search"},
        {"type": "content_contains", "path": "deployment.xml", "pattern": "<prod>", "text": "deployment.xml has <prod>"},
        {"type": "content_contains", "path": "deployment.xml", "pattern": "<region>", "text": "deployment.xml has <region>"},
        {"type": "content_contains", "path": "root.xml", "pattern": "x[384]", "text": "Query-profile type declares 384-dim tensor"}
      ],
      "llm_rubric": "Grade this Vespa Cloud application package scaffold for correctness:\n(1) services.xml has BOTH <document-api/> AND <search/> in the container cluster (omitting document-api breaks feeding).\n(2) The hugging-face-embedder component is declared inside the container cluster as <component id=\"e5\" type=\"hugging-face-embedder\"> with <transformer-model url=...> and <tokenizer-model url=...> child elements pointing at the e5-small-v2 URLs. The legacy <component class=\"...HuggingFaceEmbedder\"><config>...</config></component> form is NOT preferred — penalize that.\n(3) Vespa Cloud nodes use <resources vcpu=... memory=... disk=.../> rather than hostalias.\n(4) The <document> element inside <documents> has an explicit mode attribute ('index' is correct here — mode is required per the Vespa RNC schema).\n(5) Redundancy is declared (either <redundancy> or the preferred <min-redundancy>; per the Vespa docs, min-redundancy is now recommended over redundancy) and the value does not exceed the content node count.\n(6) Document type name in services.xml matches the schema file name AND the 'schema X { document X { } }' inside the schema file.\n(7) The schema's embedding field has 'indexing: ... embed e5 ...' or equivalent invoking the e5 component, type tensor<float>(x[384]), and HNSW configuration in its index block.\n(8) The schema's title and body fields have 'index: enable-bm25' inside their indexing/index block (BM25 ranking requires this — without it, bm25() returns 0 silently).\n(9) deployment.xml has <deployment version='1.0'>, a <prod> block, and at least one <region> child (e.g. aws-us-east-1c).\n(10) The query-profile-type at search/query-profiles/types/root.xml declares a tensor<float>(x[384]) field for the query embedding input (the < and > should be XML-escaped as &lt; / &gt; inside the type= attribute value)."
    },
    {
      "id": 2,
      "name": "fix-broken-services-xml",
      "prompt": "There is an existing services.xml in this directory. It has several issues: some break deployment outright, others violate Vespa schema requirements. Read it carefully, identify ALL the issues, and write the corrected file back to services.xml. Make MINIMAL changes — only fix what is actually broken. Do not restructure or re-style the file unnecessarily. Do not change unrelated elements (cluster names, redundancy levels, etc.) beyond what is required to make the file valid.\n\nBefore writing the fix, briefly state which issues you identified and why each is a bug.",
      "expected_output": "Fixed services.xml with three issues addressed: (1) <document-api/> added inside the container cluster (was missing — feeding to /document/v1/ would 404), (2) explicit mode attribute added to <document type=\"product\"/> — should be mode=\"index\" for normal indexed search (mode is required per the Vespa services.xml schema), (3) the redundancy-vs-node-count mismatch resolved EITHER by reducing <redundancy> from 3 to 1 (cluster has only 1 content node) OR by adding a second <node> with distribution-key=\"1\" so the redundancy of 3 can stand. The simplest minimal fix is redundancy=1.",
      "files": ["data/fixtures/fix-broken-services-xml/services.xml"],
      "assertions": [
        {"type": "file_exists", "path": "services.xml", "text": "Fixed services.xml written"},
        {"type": "content_contains", "path": "services.xml", "pattern": "<document-api/>", "text": "<document-api/> added (fixes missing feeding endpoint)"},
        {"type": "content_contains", "path": "services.xml", "pattern": "<search/>", "text": "<search/> preserved"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<document\\b(?=[^>]*\\btype=\"product\")(?=[^>]*\\bmode=\"(index|streaming|store-only)\")", "text": "<document type=\"product\"> has explicit valid mode attribute (any attribute order)"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<redundancy>\\s*1\\s*</redundancy>|distribution-key=\"1\"", "text": "Either redundancy reduced to 1 OR a second content node added (both are valid fixes)"}
      ],
      "llm_rubric": "Grade the diagnosis and fix of services.xml. Three bugs were planted; check each:\n(1) BUG: <document-api/> was missing from the container cluster, which would break feeding (404 on /document/v1/). Did the fix add <document-api/> inside <container>?\n(2) BUG: <document type=\"product\"/> had no mode attribute. The Vespa services.xml schema requires mode (valid values: index, streaming, store-only). Did the fix add a sensible mode (index is correct for a typical product search)?\n(3) BUG: <redundancy>3</redundancy> with only 1 content node. Vespa silently clamps the effective redundancy to the node count, so this is not a hard validation failure but it does mean the configured redundancy is not honored. Did the fix EITHER reduce redundancy to 1 OR add additional <node> elements (with distribution-key=\"1\", etc.) to actually support redundancy=3? Either resolution is acceptable.\n(4) Were the changes MINIMAL? The fix should not have rewritten unrelated parts (e.g., changed the container id, removed the existing <search/>, restyled XML, added unrelated elements like <document-processing/>).\n(5) BONUS: Did the model briefly explain WHICH issues it identified and why each is a bug, before writing the fix (the prompt asks for this)? Penalize if the model just produced a fixed file with no diagnostic commentary.\n(6) PENALTY if the model invented spurious 'bugs' that aren't actually problems (e.g., complaining about the content cluster id 'my-content' — hyphens are allowed by the Vespa RNC schema and the official sizing-examples docs use them; treat this only as a soft style preference, not a real bug)."
    },
    {
      "id": 3,
      "name": "multi-cluster-with-document-modes",
      "prompt": "Create a file named `services.xml` in the current working directory for a Vespa Cloud application that combines THREE document types across TWO content clusters:\n\n- Content cluster #1 ('catalog'): serves 'product' documents in normal indexed mode (used for full-text + filter search).\n- Content cluster #2 ('user_data'): serves TWO document types in different modes:\n    * 'user_session' — should use streaming mode (per-user session data; queries always target a single user via streaming.groupname, so streaming is the right mode).\n    * 'archived_log' — should use store-only mode (only ever fetched by document ID, never searched).\n\nAlso include:\n- One container cluster (id='default') exposing both feeding and search endpoints.\n- Vespa Cloud <resources> specs on every cluster (you choose reasonable vcpu/memory/disk values).\n- Production redundancy of 2 on each content cluster.\n\nSave the complete services.xml to disk — do not just print it in your response. Do not write any schema files; just the services.xml.",
      "expected_output": "services.xml with: container cluster (default) declaring <document-api/>, <search/>, and <resources> on its <nodes>; two <content> clusters — one with id='catalog' serving <document type=\"product\" mode=\"index\"/>, one with id='user_data' serving <document type=\"user_session\" mode=\"streaming\"/> and <document type=\"archived_log\" mode=\"store-only\"/>; <redundancy>2</redundancy> (or <min-redundancy>2</min-redundancy>) on both content clusters; <resources> on every <nodes> block.",
      "files": [],
      "assertions": [
        {"type": "file_exists", "path": "services.xml", "text": "services.xml created"},
        {"type": "content_contains", "path": "services.xml", "pattern": "<document-api/>", "text": "<document-api/> declared"},
        {"type": "content_contains", "path": "services.xml", "pattern": "<search/>", "text": "<search/> declared"},
        {"type": "content_contains", "path": "services.xml", "pattern": "mode=\"streaming\"", "text": "streaming mode used"},
        {"type": "content_contains", "path": "services.xml", "pattern": "mode=\"store-only\"", "text": "store-only mode used"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<content\\b[^>]*\\bid=\"catalog\"", "text": "Content cluster 'catalog' present"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<content\\b[^>]*\\bid=\"user_data\"", "text": "Content cluster 'user_data' present"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<content\\b[\\s\\S]*?</content>[\\s\\S]*?<content\\b", "text": "Two distinct <content>...</content> clusters present"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<(min-)?redundancy>\\s*2\\s*</(min-)?redundancy>", "text": "Redundancy of 2 on content clusters (redundancy or min-redundancy)"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<resources\\b[^>]*\\bvcpu=", "text": "Vespa Cloud <resources> with vcpu attribute"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<document\\b(?=[^>]*\\btype=\"product\")(?=[^>]*\\bmode=\"index\")", "text": "product document declared with index mode (any attribute order)"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<document\\b(?=[^>]*\\btype=\"user_session\")(?=[^>]*\\bmode=\"streaming\")", "text": "user_session document declared with streaming mode (any attribute order)"},
        {"type": "content_matches", "path": "services.xml", "pattern": "<document\\b(?=[^>]*\\btype=\"archived_log\")(?=[^>]*\\bmode=\"store-only\")", "text": "archived_log document declared with store-only mode (any attribute order)"}
      ],
      "llm_rubric": "Grade this multi-cluster services.xml:\n(1) Are there exactly TWO <content> blocks (not one combined, not three) — one for 'catalog' and one for 'user_data'?\n(2) Is the 'catalog' cluster serving exactly the 'product' document type with mode='index'?\n(3) Does the 'user_data' cluster serve BOTH 'user_session' (mode='streaming') AND 'archived_log' (mode='store-only') in the same <documents> block?\n(4) Are mode attributes correct: index for product, streaming for user_session (per-user data is the documented use case for streaming search via streaming.groupname), store-only for archived_log (no search backend, only fetched by document ID)?\n(5) Does each <content> cluster have <redundancy>2</redundancy> or <min-redundancy>2</min-redundancy> (the latter is now preferred per the Vespa docs)?\n(6) Does each cluster's <nodes> have <resources vcpu=... memory=... disk=.../> (since we asked for Vespa Cloud)?\n(7) Is the container cluster declaring BOTH <document-api/> and <search/> (omitting document-api would break feeding)?\n(8) PENALTY if extra schema files were written, or if all three document types were jammed into a single content cluster, or if document modes were swapped."
    },
    {
      "id": 4,
      "name": "add-query-profile-with-tensor-type",
      "prompt": "An existing Vespa application package is in the current working directory. It already has `services.xml` and `schemas/article.sd`. The schema's `semantic` rank profile declares an input `query(q_embedding) tensor<float>(x[384])`, but no query profile exists yet — meaning callers cannot pass a tensor value to `ranking.features.query(q_embedding)` because Vespa rejects untyped tensor parameters.\n\nAdd the query-profile configuration files needed so callers CAN pass a 384-dim float tensor as the query embedding. Specifically:\n\n1. Create `search/query-profiles/default.xml` — a default query profile linked to a custom type named 'root' (via the `type=\"root\"` attribute). Inside the profile, set a default field: `<field name=\"ranking.profile\">semantic</field>` (so queries default to the semantic rank profile that was already defined in the schema).\n\n2. Create `search/query-profiles/types/root.xml` — a query-profile-type declaration with id 'root' that declares a field named EXACTLY `ranking.features.query(q_embedding)` with type `tensor<float>(x[384])`. Remember that XML attribute values cannot contain raw `<` and `>` characters — the type value must use the entities `&lt;` and `&gt;` (so the type attribute reads `type=\"tensor&lt;float&gt;(x[384])\"`).\n\nDo not modify the existing services.xml or schemas/article.sd. Do not create any other files.\n\nBefore writing, briefly explain why both files are needed and how they link together.",
      "expected_output": "Two new files. search/query-profiles/default.xml: <query-profile id=\"default\" type=\"root\"> with <field name=\"ranking.profile\">semantic</field>. search/query-profiles/types/root.xml: <query-profile-type id=\"root\"> with <field name=\"ranking.features.query(q_embedding)\" type=\"tensor&lt;float&gt;(x[384])\"/>. The default profile references the type via type=\"root\"; the type allows the tensor query input to be passed through ranking.features.query().",
      "files": [
        "data/fixtures/add-query-profile-with-tensor-type/services.xml",
        "data/fixtures/add-query-profile-with-tensor-type/schemas/article.sd"
      ],
      "assertions": [
        {"type": "file_exists", "path": "search/query-profiles/default.xml", "text": "search/query-profiles/default.xml created"},
        {"type": "file_exists", "path": "search/query-profiles/types/root.xml", "text": "search/query-profiles/types/root.xml created"},
        {"type": "content_matches", "path": "search/query-profiles/default.xml", "pattern": "<query-profile\\b(?=[^>]*\\bid=\"default\")(?=[^>]*\\btype=\"root\")", "text": "default.xml is <query-profile id=\"default\" type=\"root\"> (links to the type via type=)"},
        {"type": "content_matches", "path": "search/query-profiles/default.xml", "pattern": "<field\\b[^>]*\\bname=\"ranking\\.profile\"[^>]*>\\s*semantic\\s*</field>", "text": "default.xml defaults ranking.profile to 'semantic'"},
        {"type": "content_matches", "path": "search/query-profiles/types/root.xml", "pattern": "<query-profile-type\\b[^>]*\\bid=\"root\"", "text": "root.xml is <query-profile-type id=\"root\">"},
        {"type": "content_matches", "path": "search/query-profiles/types/root.xml", "pattern": "name=\"ranking\\.features\\.query\\(q_embedding\\)\"", "text": "Field name uses exact ranking.features.query(q_embedding) form"},
        {"type": "content_matches", "path": "search/query-profiles/types/root.xml", "pattern": "type=\"tensor&lt;float&gt;\\(x\\[384\\]\\)\"", "text": "type value uses XML-escaped &lt;/&gt; (literal < > in an XML attribute is malformed)"},
        {"type": "file_exists", "path": "services.xml", "text": "Original services.xml preserved"},
        {"type": "file_exists", "path": "schemas/article.sd", "text": "Original article.sd preserved"}
      ],
      "llm_rubric": "Grade these query-profile additions:\n(1) Does `search/query-profiles/default.xml` declare a `<query-profile id=\"default\" type=\"root\">`? Both attributes are required — without `type=\"root\"`, the default profile doesn't pick up the type declarations.\n(2) Does it set `<field name=\"ranking.profile\">semantic</field>` so callers default to the semantic rank profile?\n(3) Does `search/query-profiles/types/root.xml` declare `<query-profile-type id=\"root\">` and contain a `<field name=\"ranking.features.query(q_embedding)\" type=\"...\"/>` element with the EXACT field name `ranking.features.query(q_embedding)`? The exact spelling matters because rank profile inputs are looked up by name.\n(4) Is the tensor type written with XML entity escapes: `type=\"tensor&lt;float&gt;(x[384])\"`? Raw `<` and `>` inside an XML attribute value is a parse error — the file would not load.\n(5) Was the existing services.xml and schemas/article.sd left UNCHANGED? Penalize if the agent rewrote either file.\n(6) Were exactly the two requested files created (no extras like a redundant query-profile types subfolder, no unrelated component declarations)?\n(7) BONUS: Did the agent briefly explain that the type is what allows tensor-typed query parameters to flow through (without a declared type, Vespa cannot interpret the inbound payload as a tensor — it falls back to treating it as a string)?\n(8) PENALTY for incorrect linking: declaring the type but forgetting `type=\"root\"` on the default profile, or putting the type definition inline in default.xml instead of in the types/ folder."
    },
    {
      "id": 5,
      "name": "deployment-xml-multi-region-with-tests",
      "prompt": "Create a `deployment.xml` file in the current working directory for a Vespa Cloud deployment with the following requirements:\n\n- A single instance named `default`.\n- Automatic test runs before any production deploy: include both `<test/>` (system tests) AND `<staging/>` (staging-environment tests) as siblings inside the instance.\n- Three production regions, in this order: `aws-us-east-1c`, `aws-eu-west-1a`, `gcp-us-central1-f`. (Use these EXACT region strings.)\n- The XML must start with `<?xml version=\"1.0\" encoding=\"utf-8\" ?>` and the root element must be `<deployment version=\"1.0\">`.\n- All three region elements go inside a SINGLE `<prod>` block, NOT one prod block per region.\n\nDo not include `<test/>` or `<staging/>` blocks AFTER the `<prod>` block — they go BEFORE prod (Vespa's deployment pipeline runs tests first, then deploys to prod). The order inside `<instance>` should be: <test/>, <staging/>, <prod>...</prod>.\n\nSave the file to disk. Do not print it in chat.",
      "expected_output": "deployment.xml with: <?xml ...?> declaration; root <deployment version=\"1.0\">; one <instance id=\"default\"> containing <test/>, <staging/>, and a single <prod> block with three <region> children in order: aws-us-east-1c, aws-eu-west-1a, gcp-us-central1-f.",
      "files": [],
      "assertions": [
        {"type": "file_exists", "path": "deployment.xml", "text": "deployment.xml created"},
        {"type": "content_matches", "path": "deployment.xml", "pattern": "<\\?xml\\s+version=\"1\\.0\"", "text": "XML declaration present"},
        {"type": "content_matches", "path": "deployment.xml", "pattern": "<deployment\\b[^>]*\\bversion=\"1\\.0\"", "text": "<deployment version=\"1.0\"> root element"},
        {"type": "content_matches", "path": "deployment.xml", "pattern": "<instance\\b[^>]*\\bid=\"default\"", "text": "<instance id=\"default\"> declared"},
        {"type": "content_contains", "path": "deployment.xml", "pattern": "<test/>", "text": "<test/> block present"},
        {"type": "content_contains", "path": "deployment.xml", "pattern": "<staging/>", "text": "<staging/> block present"},
        {"type": "content_matches", "path": "deployment.xml", "pattern": "<test/>[\\s\\S]*?<staging/>[\\s\\S]*?<prod>", "text": "Order: <test/> before <staging/> before <prod> (tests run before prod deploy)"},
        {"type": "content_matches", "path": "deployment.xml", "pattern": "<region>\\s*aws-us-east-1c\\s*</region>", "text": "Region aws-us-east-1c present"},
        {"type": "content_matches", "path": "deployment.xml", "pattern": "<region>\\s*aws-eu-west-1a\\s*</region>", "text": "Region aws-eu-west-1a present"},
        {"type": "content_matches", "path": "deployment.xml", "pattern": "<region>\\s*gcp-us-central1-f\\s*</region>", "text": "Region gcp-us-central1-f present"},
        {"type": "content_matches", "path": "deployment.xml", "pattern": "<prod>[\\s\\S]*aws-us-east-1c[\\s\\S]*aws-eu-west-1a[\\s\\S]*gcp-us-central1-f[\\s\\S]*</prod>", "text": "All three regions inside a single <prod>...</prod> block in the requested order"}
      ],
      "llm_rubric": "Grade this deployment.xml:\n(1) Is the root element `<deployment version=\"1.0\">`? The version attribute is required.\n(2) Is there exactly ONE `<instance id=\"default\">` block? Multiple instances or omitting the instance wrapper are wrong here.\n(3) Are BOTH `<test/>` and `<staging/>` present? `<test/>` runs system tests, `<staging/>` runs staging tests — they're independent stages, and the prompt asked for both.\n(4) Are `<test/>` and `<staging/>` placed BEFORE the `<prod>` block? Test stages run first in Vespa Cloud's deployment pipeline; placing them after prod is structurally wrong (and the prompt explicitly asks for this order).\n(5) Are all three regions inside a SINGLE `<prod>...</prod>` block (not one prod per region)? The deployment.xml schema expects a single prod block enumerating regions.\n(6) Are the three regions present in the exact spellings: aws-us-east-1c, aws-eu-west-1a, gcp-us-central1-f? Penalize for typos like 'us-east-1' (the AZ suffix is required for Vespa Cloud regions).\n(7) Are the regions in the requested ORDER (aws-us-east-1c first)? Deployment order matters because Vespa Cloud rolls regions out sequentially by default.\n(8) PENALTY for: inventing unrelated blocks (e.g. `<dev>`, `<perf>`), using `<region environment=\"prod\">` style (that's an older form), or adding bcp/policy/notifications blocks that weren't asked for."
    }
  ]
}
