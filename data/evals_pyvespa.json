{
  "skill_name": "pyvespa",
  "skill_path": "pyvespa",
  "evals": [
    {
      "id": 1,
      "name": "build-app-package-script",
      "prompt": "Write a Python script named `build_app.py` in the current working directory that uses pyvespa (`vespa.package`) to programmatically build an ApplicationPackage for hybrid search over 'article' documents.\n\nThe script will be REVIEWED for correctness — do not try to execute it; pyvespa may not be installed in this environment, and the script just needs to be syntactically and semantically correct against the pyvespa API.\n\nRequirements for the package:\n\n- Application name: `articles` (must follow pyvespa naming rules — lowercase [a-z0-9], letter-start, max 20 chars, no hyphens or underscores. The ApplicationPackage constructor enforces this with a ValueError at construction time per pyvespa source).\n- A single schema named `article` (created automatically by ApplicationPackage).\n- Document fields:\n  * `title` (string) — full-text searchable with BM25 (use `index=\"enable-bm25\"`).\n  * `body` (string) — full-text searchable with BM25.\n  * `category` (string) — used as an attribute filter with fast-search (note: pyvespa `Field` takes `attribute` as a LIST per the Field signature).\n  * `embedding` (tensor<float>(x[384])) — generated by piping `title` and `body` through an embedder component with id `e5`. Use the multiline/embed indexing-pipeline form, which in pyvespa requires a TUPLE (not a list) for the `indexing=` argument — tuples render as multiline `indexing { ... }` blocks; lists render as pipe-separated single-line directives.\n  * The embedding field must use HNSW with `distance_metric=\"angular\"`.\n- A `default` fieldset over `title` and `body`.\n- A rank profile named `hybrid` declaring an input `query(q) tensor<float>(x[384])` and a first-phase expression combining `bm25(title)`, `bm25(body)`, and `closeness(field, embedding)`.\n\nThe script should end by calling `app.to_files(\"./output\")` (so that if executed it would generate the package files), but executing it is NOT required.\n\nWrite the script to disk; do not paste it in chat.",
      "expected_output": "build_app.py importing from vespa.package (ApplicationPackage, Field, FieldSet, HNSW, RankProfile), defining a function or top-level block that creates ApplicationPackage(name='articles'), adds 4 fields with correct indexing/index/attribute/ann arguments (attribute as list, multiline embed-indexing as tuple), a fieldset, a RankProfile with inputs and first_phase, and calls app.to_files('./output'). The script's correctness is graded statically; execution is not required.",
      "files": [],
      "assertions": [
        {"type": "file_exists", "path": "build_app.py", "text": "build_app.py created"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "from\\s+vespa\\.package\\s+import\\b", "text": "Imports from vespa.package"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "ApplicationPackage\\s*\\(\\s*name\\s*=\\s*[\"']articles[\"']", "text": "ApplicationPackage created with name='articles' (no hyphens/underscores)"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "Field\\s*\\([\\s\\S]*?name\\s*=\\s*[\"']title[\"']", "text": "Field for title declared"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "Field\\s*\\([\\s\\S]*?name\\s*=\\s*[\"']embedding[\"'][\\s\\S]*?type\\s*=\\s*[\"']tensor<float>\\(x\\[384\\]\\)[\"']", "text": "embedding Field declared with tensor<float>(x[384])"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "HNSW\\s*\\([^)]*distance_metric\\s*=\\s*[\"']angular[\"']", "text": "HNSW with distance_metric='angular'"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "index\\s*=\\s*[\"']enable-bm25[\"']", "text": "BM25 enabled on text fields (index='enable-bm25')"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "attribute\\s*=\\s*\\[[\"']fast-search[\"']\\]", "text": "category Field uses attribute=['fast-search'] (LIST, not bare string)"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "indexing\\s*=\\s*\\([\\s\\S]*?embed\\s+e5[\\s\\S]*?\\)", "text": "embedding Field uses a TUPLE for indexing (any tuple containing 'embed e5' between parens, single-line or multi-line, single-string or multi-string)"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "RankProfile\\s*\\([\\s\\S]*?name\\s*=\\s*[\"']hybrid[\"']", "text": "RankProfile named 'hybrid'"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "inputs\\s*=\\s*\\[[\\s\\S]*?\\(\\s*[\"']query\\(q\\)[\"']\\s*,\\s*[\"']tensor<float>\\(x\\[384\\]\\)[\"']", "text": "RankProfile declares input query(q) tensor<float>(x[384])"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "first_phase\\s*=\\s*\\(?\\s*[\"'][\\s\\S]*?bm25\\(title\\)[\\s\\S]*?closeness\\(field,\\s*embedding\\)", "text": "first_phase combines bm25 and closeness (accepts string or parenthesized string form)"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "to_files\\s*\\(\\s*[\"']\\./output[\"']", "text": "Calls app.to_files('./output')"}
      ],
      "llm_rubric": "Grade this pyvespa script for correctness against the pyvespa source at repo location (try to find it locally, or ask user if it is OK to clone from [pyvespa](https://github.com/vespa-engine/pyvespa.git):\n(1) Does the script import from `vespa.package` (NOT `vespa.deployment`) the classes needed: ApplicationPackage, Field, FieldSet, HNSW, RankProfile? Penalize wrong-module imports.\n(2) Is `ApplicationPackage(name='articles')` used — name follows pyvespa rules: lowercase [a-z0-9], starts with letter, no hyphens or underscores, ≤20 chars (the ApplicationPackage __init__ enforces this with a ValueError per pyvespa source)?\n(3) For the `category` field, is `attribute` passed as a LIST: `attribute=['fast-search']`? Per the Field signature `attribute: Optional[List[str]] = None`, a bare string like `attribute='fast-search'` is a type mismatch.\n(4) For the `embedding` field with embed pipeline, is `indexing` passed as a TUPLE (e.g. `indexing=('input title . \" \" . input body | embed e5 | index | attribute',)`) — NOT a list? Per `Field.indexing_to_text` in pyvespa source, tuples are detected and rendered as multiline `indexing { ... }` blocks; lists render as pipe-separated `indexing: a | b | c` directives. The single-element list `['input title | embed e5 | ...']` would still parse acceptably because the `|`s are inside the string, but the documented convention (gotcha #2 in pyvespa SKILL.md) is to use a tuple for multiline/embed expressions.\n(5) Is BM25 enabled on title/body via `index='enable-bm25'`? Without this, bm25() returns 0 silently.\n(6) Does the rank profile declare `inputs=[('query(q)', 'tensor<float>(x[384])')]` and `first_phase` referencing both bm25 and closeness?\n(7) PENALTY for invented or wrong APIs: `app.add_field()` (no such method — must be `app.schema.add_fields(...)`), `is_successfull()` (deprecated misspelling — `is_successful` is the canonical spelling per vespa/io.py), or passing the wrong types to Field constructor."
    },
    {
      "id": 2,
      "name": "feed-and-query-script",
      "prompt": "Write a Python script named `feed_query.py` in the current working directory that uses pyvespa to feed documents and run a hybrid query against an ALREADY-RUNNING Vespa instance at `http://localhost:8080`. Do NOT deploy a new instance and do NOT actually execute the script — just write it to disk. The script will be reviewed for correctness.\n\nThe script must:\n\n1. Connect to the running Vespa instance:\n   ```python\n   from vespa.application import Vespa\n   app = Vespa(url=\"http://localhost:8080\")\n   ```\n2. Define a list `docs` of 3 example documents for schema `article`. Each item must be a dict with keys `id` (str) and `fields` (dict). The `fields` dict must include at least: `title` (str), `body` (str), and `embedding` (a list of 384 floats — you may use `[0.1] * 384` as a placeholder).\n3. Bulk-feed these documents asynchronously using `app.feed_async_iterable(docs, schema='article', callback=...)`. Provide a callback function that prints any failure. The callback signature is `callback(response, doc_id)`.\n4. Inside an `app.syncio()` context manager, run a HYBRID query:\n   - `yql`: `select * from sources * where userQuery() or ({targetHits:10}nearestNeighbor(embedding, q))`\n   - `query`: a free-text search string (e.g. 'machine learning')\n   - `ranking`: name of a rank profile (use 'hybrid')\n   - `input.query(q)`: a 384-float list (e.g. `[0.1] * 384`)\n   - `hits`: 10\n5. Iterate `response.hits` and print each hit's title and relevance score. Use `response.is_successful()` (NOT the misspelled `is_successfull()`) before iterating.\n\nWrite ONLY the script. Do not run it (Vespa is not actually running in this environment). Do not print the contents in chat.",
      "expected_output": "feed_query.py with: import Vespa from vespa.application; Vespa(url='http://localhost:8080'); docs list with id+fields dicts containing title/body/embedding; app.feed_async_iterable(docs, schema='article', callback=callback_fn); with app.syncio() as sess block running sess.query() with yql, query, ranking='hybrid', input.query(q) 384-float, hits=10; response.is_successful() check; iteration over response.hits printing title and relevance.",
      "files": [],
      "assertions": [
        {"type": "file_exists", "path": "feed_query.py", "text": "feed_query.py created"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "from\\s+vespa\\.application\\s+import\\s+Vespa\\b", "text": "Imports Vespa from vespa.application"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "Vespa\\s*\\(\\s*url\\s*=\\s*[\"']http://localhost:8080[\"']", "text": "Connects to http://localhost:8080"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "feed_async_iterable\\s*\\(", "text": "Uses feed_async_iterable for bulk feed"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "schema\\s*=\\s*[\"']article[\"']", "text": "Schema name 'article' passed to feed/query"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "callback\\s*=\\s*\\w+", "text": "callback argument provided to feed_async_iterable"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "with\\s+app\\.syncio\\s*\\(", "text": "Uses app.syncio() context manager"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "userQuery\\(\\)[\\s\\S]*?nearestNeighbor\\(\\s*embedding\\s*,\\s*q\\s*\\)", "text": "Hybrid YQL combines userQuery() and nearestNeighbor"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "\\{targetHits:\\s*10\\}", "text": "nearestNeighbor uses targetHits:10 annotation"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "([\"']ranking[\"']\\s*:\\s*[\"']hybrid[\"']|\\branking\\s*=\\s*[\"']hybrid[\"'])", "text": "Query sets ranking='hybrid' (either as a body dict entry \"ranking\": \"hybrid\" or as a kwarg ranking=\"hybrid\")"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "[\"']input\\.query\\(q\\)[\"']", "text": "Query body passes input.query(q)"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "is_successful\\s*\\(\\s*\\)", "text": "Uses is_successful() (NOT the deprecated misspelling is_successfull)"},
        {"type": "content_matches", "path": "feed_query.py", "pattern": "response\\.hits", "text": "Iterates response.hits"}
      ],
      "llm_rubric": "Grade this pyvespa feed+query script:\n(1) Does the script import Vespa from `vespa.application` (not from `vespa.deployment` — Vespa is the connection class)?\n(2) Are docs structured as `{\"id\": str, \"fields\": dict}` — the documented bulk-feed data format? `{\"data_id\": ...}` or `{\"key\": ...}` would be wrong.\n(3) Is `feed_async_iterable` used for bulk feed (preferred for throughput per gotcha #5 in pyvespa SKILL.md), with both `schema=` and `callback=` kwargs? Note that `feed_iterable` (sync, thread-pooled) is also acceptable but less idiomatic for bulk.\n(4) Is the query running INSIDE an `app.syncio()` context manager (gotcha #6 — always use context managers for connection mgmt)? A raw `app.query(...)` outside any context is less idiomatic and should be penalized lightly.\n(5) Is the YQL hybrid query correctly written: `select * from sources * where userQuery() or ({targetHits:10}nearestNeighbor(embedding, q))`? Check that `nearestNeighbor` references the document tensor field `embedding` and the query tensor `q` (NOT q_embedding or some other name — the prompt specifies `q`).\n(6) Does the query body set `ranking='hybrid'` and pass `input.query(q)` as a 384-float list?\n(7) Is `response.is_successful()` (correct spelling) used before iterating? The deprecated misspelled `is_successfull()` is wrong (gotcha #7).\n(8) PENALTY if the agent: (a) tried to actually run the script and threw a connection error (the prompt asked not to run it); (b) used `app.feed_data_point` for the bulk feed instead of `feed_async_iterable`/`feed_iterable`; (c) confused the document tensor field name with the query tensor input name."
    },
    {
      "id": 3,
      "name": "fix-broken-pyvespa-script",
      "prompt": "There is a Python script named `build_app.py` in the current working directory that uses pyvespa to build an ApplicationPackage. It has several bugs — some break execution outright, others produce subtly wrong .sd output. Read the file, identify every bug, and overwrite `build_app.py` in place with a fixed version.\n\nMake MINIMAL changes — only fix what is actually broken. Do not restructure the script, add new fields, or remove unrelated lines.\n\nBefore writing the fix, briefly list each bug you found and the fix you applied.",
      "expected_output": "Fixed build_app.py. Bugs that should be fixed: (1) ApplicationPackage name 'my-search-app' has a hyphen which violates pyvespa name rules (lowercase [a-z0-9], no hyphens) — must change to e.g. 'mysearchapp' or 'searchapp'. (2) The category Field has attribute='fast-search' as a bare string; pyvespa's Field constructor expects attribute as a LIST: attribute=['fast-search']. (3) The embedding Field has indexing=['...embed e5...'] as a LIST containing the embed expression; for multiline/embed indexing pipelines, pyvespa requires a TUPLE: indexing=('input title | embed e5 | index | attribute',). The list form renders the embed expression as a single literal directive rather than the multiline indexing block needed.",
      "files": ["data/fixtures/fix-broken-pyvespa-script/build_app.py"],
      "assertions": [
        {"type": "file_exists", "path": "build_app.py", "text": "Fixed build_app.py written"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "ApplicationPackage\\s*\\(\\s*name\\s*=\\s*[\"'][a-z][a-z0-9]{0,19}[\"']", "text": "App name fixed to lowercase [a-z0-9] (≤20 chars, no hyphen/underscore)"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "attribute\\s*=\\s*\\[[\"']fast-search[\"']\\]", "text": "category Field uses attribute=['fast-search'] (LIST, not bare string)"},
        {"type": "content_matches", "path": "build_app.py", "pattern": "indexing\\s*=\\s*\\([\\s\\S]*?embed\\s+e5[\\s\\S]*?\\)", "text": "embedding Field uses a TUPLE for indexing (any tuple containing 'embed e5' between parens)"}
      ],
      "llm_rubric": "Grade the diagnosis and fix of build_app.py. Three bugs were planted; check each:\n(1) BUG: `ApplicationPackage(name='my-search-app')` — hyphens are not allowed by pyvespa's name rules (lowercase [a-z0-9] only, must start with letter, max 20 chars per gotcha #1). Did the fix change the name to a valid form (e.g. 'mysearchapp', 'searchapp')? Penalize if the fix introduced an underscore (also disallowed).\n(2) BUG: `Field(name='category', ..., attribute='fast-search')` — pyvespa's Field constructor requires `attribute` to be a LIST. Bare string is a type mismatch. Did the fix change it to `attribute=['fast-search']`?\n(3) BUG: `Field(name='embedding', ..., indexing=['input title | embed e5 | index | attribute'])` — for multiline/embed indexing expressions, pyvespa expects a TUPLE (renders as a multi-line `indexing { ... }` block), NOT a list (which renders as pipe-separated directives, mangling the embed pipeline). Did the fix change the brackets `[...]` to a single-element tuple `(...,)` (with the trailing comma — important to remain a tuple)? This is gotcha #2 in the pyvespa SKILL.md.\n(4) Were the changes MINIMAL? The agent should not add fields, change unrelated arguments, or restructure the script.\n(5) BONUS: Did the agent list the three bugs and their fixes before writing the file (the prompt asks for this)?\n(6) PENALTY if the agent invented bugs that weren't there (e.g., complaining that ann=HNSW(distance_metric='angular') should be prenormalized-angular — angular is a valid documented distance_metric and was not flagged in the prompt as wrong)."
    }
  ]
}
